partial = @columns ? "generated_queries/wizard_2b/model_columns_node" : "generated_queries/wizard_2a/model_node"

query_generator_session.used_models.each do |model|
  page[model_dom_id(model)].replace_html model_node(model, partial)
end

page.call "queryGenerator.graph.repaintConnections"
