%table
  %thead
    %tr
      %th.handle{:colspan => 3}
        = human_model_name(model)

  %tbody
    = render :partial => wizard_file(3, "model_column_row"), :collection => model.columns, :as => :column, :locals => {:model => model}