%table
  %thead
    %tr
      - @generated_query.table_header_columns.each do |header|
        %th= header
  %tbody
    - @executed_query_rows.each do |row|
      %tr
        - row.each do |column|
          %td= column

= will_paginate @executed_query_rows